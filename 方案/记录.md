API服务是一个已编译的Go程序liquid-api，推理服务是Python程序
原系统使用的是标准的PyQt5信号连接模式：

信号定义方式：使用 QtCore.Signal 定义信号
信号连接方式：使用 .connect() 方法连接信号到槽函数
websocket网络连接相关的不应该放在network文件夹吗F:\liquid_detect\system_client_sever\client\network，其余代码只负责发送指令
F:\liquid_detect\system_client_sever\server\start_websocket_server.py，F:\liquid_detect\system_client_sever\server\temp_start.py这俩临时脚本用处是什么？
服务启动流程
客户端启动时检查SSH连接
检查远程服务器上的服务状态
如果需要，通过SSH启动远程服务
客户端直接连接到ws://192.168.0.121:8085
负责从RTSP流获取视频帧，进行液位检测，并通过WebSocket推送检测结果
1负责从RTSP流获取视频帧，独立设置py文件在video文件夹，实现过程参考客户端显示相机画面，只不过使用的库不一样
2F:\liquid_detect\system_client_sever\server\detection\detection.py进行液位检测
通过WebSocket推送检测结果
3通过WebSocket推送检测结果归档liquid_detect\system_client_sever\server\websocket
服务端不需要显示视频画面，只需要把视频画面送入模型进行检测F:\liquid_detect\system_client_sever\server\detection\detection.py，输入为帧和标注信息，输出为液位高度信息，测试该功能
系统使用的是CUDA 11.7
生成采用pyqt5开发本系统和采用c/s架构开发本系统的svg对比图，pyqt5采用纯qt信号与槽机制，而c/s架构在此机制上在涉及到服务器的部分引入了go api和websocket用于连接服务器与客户端的数据交互，配色采用马卡龙配色，背景白色，字体黑色，上下垂直布局