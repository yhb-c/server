1.将画面渲染去除，客户端无gpu不需要这个功能
2.将检测线程去掉，客户端只需要接收服务端的信息
3.将捕获线程和显示线程合并为一个线程
4.存储线程应用最新的线程
5.目前只保留存储线程，捕获显示线程，

客户端不再：
加载模型
读取帧进行检测
使用GPU进行推理
渲染检测结果


总结
现在共有4个线程
1.接收线程：接受来自服务端的数据
2.存储线程：存储数据并写入csv
3.显示线程：未作改变
4.曲线线程：未作改变

admin:cei345678@192.168.0.27:8000/stream1


完整的4路视频流
RTSP流 → HKcapture(海康SDK) → PlayCtrl解码 → 直接渲染到HWND窗口句柄
                                              ↓
                                    显示在VideoRenderWidget上

【摄像头192.168.0.27】
         ↓
    RTSP主码流（1080P@25fps，H.264）
         ↓
    网络传输（4-8Mbps）
         ↓
【NET SDK - 4个独立连接】
    连接1 → 取流1 → 回调1
    连接2 → 取流2 → 回调2
    连接3 → 取流3 → 回调3
    连接4 → 取流4 → 回调4
         ↓
【PlayCtrl SDK - 4个独立解码器】
    解码器1（端口1）→ CPU解码 → YUV数据 → GDI渲染 → HWND1
    解码器2（端口2）→ CPU解码 → YUV数据 → GDI渲染 → HWND2
    解码器3（端口3）→ CPU解码 → YUV数据 → GDI渲染 → HWND3
    解码器4（端口4）→ CPU解码 → YUV数据 → GDI渲染 → HWND4
         ↓
【Qt窗口显示】
    QLabel1（640×360）显示摄像头1
    QLabel2（640×360）显示摄像头2
    QLabel3（640×360）显示摄像头3
    QLabel4（640×360）显示摄像头4




错误用法


    1. 使用QPixmap渲染方式（不是HWND渲染）


# channelpanel.py:558-618
def displayFrame(self, frame):
    # numpy数组 → BGR转RGB → QImage → QPixmap → 显示
    rgb_frame = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)
    q_image = QtGui.QImage(rgb_frame.data, ...)
    pixmap = QtGui.QPixmap.fromImage(q_image)
    self.videoWidget.setPixmap(pixmap)
2. 每一帧都经过Python处理

HKcapture解码回调：YUV → BGR（CPU）
显示线程：读取BGR帧
主线程：BGR → RGB → QImage → QPixmap（CPU）
Qt绘制：QPixmap → 窗口（CPU/GPU）
3. 数据格式转换链


H.264压缩流（100-500KB）
    ↓ PlayCtrl解码
YUV I420（3MB）
    ↓ cv2.cvtColor
BGR numpy数组（6MB）
    ↓ 传递到主线程
BGR numpy数组（6MB）
    ↓ cv2.cvtColor
RGB numpy数组（6MB）
    ↓ QImage
QImage对象
    ↓ QPixmap
QPixmap对象（缩放后）
    ↓ QPainter
窗口显示
4. CPU占用高的原因

4路视频同时显示时的CPU负载：


每路视频（25fps）：
1. PlayCtrl解码：YUV（CPU 20-30%）
2. YUV→BGR转换：cv2.cvtColor（CPU 5-10%）
3. 内存拷贝：6MB数据传递（CPU 2-5%）
4. BGR→RGB转换：cv2.cvtColor（CPU 5-10%）
5. QImage创建：内存拷贝（CPU 2-5%）
6. QPixmap转换：（CPU 2-5%）
7. 缩放：scaled()（CPU 5-10%）
8. 绘制：drawPixmap（CPU 2-5%）

单路总计：CPU 43-80%
4路总计：CPU 172-320%（严重超负荷）
